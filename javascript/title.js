window.addEventListener('DOMContentLoaded', () => {
    // Wait 2 seconds, then clean up the title for the user
    setTimeout(() => {
        document.title = "Dylan Ward Studios";
    }, 2000); 
});
